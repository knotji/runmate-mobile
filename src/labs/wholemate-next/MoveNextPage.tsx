import { LabComingSoon } from './LabComingSoon';

const MoveNextPage: React.FC = () => (
  <LabComingSoon
    tab="move"
    title="Move"
    note="Centers on today's plan and a movement timeline. Designed after Health, once the modular-dashboard language is settled."
  />
);

export default MoveNextPage;
