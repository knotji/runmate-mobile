import { LabComingSoon } from './LabComingSoon';

const YouNextPage: React.FC = () => (
  <LabComingSoon
    tab="you"
    title="You"
    note="Goals, context, reflection, and settings — the destination that replaces Coach as a primary tab. Designed last."
  />
);

export default YouNextPage;
